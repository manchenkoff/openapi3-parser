requests:
  LimitOffsetRequest:
    title: LimitOffsetRequest
    type: object
    required:
      - limit
      - offset
    properties:
      limit:
        type: integer
        example: 1
      offset:
        type: integer
        example: 1

schemas:
  User:
    type: object
    required:
      - login
    properties:
      login:
        type: string
        example: 'John Doe'
        description: 'User login'